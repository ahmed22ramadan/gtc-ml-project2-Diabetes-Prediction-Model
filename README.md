# 🩺 Diabetes Prediction Model

This project is a **Machine Learning web app** built with **Streamlit** to predict whether a patient is **Diabetic** or **Non-Diabetic** based on diagnostic measurements.

---

## 📌 Features
- Exploratory Data Analysis (EDA) with charts and insights.
- Data preprocessing (standardization, train-test split).
- Multiple machine learning models implemented:
  - Logistic Regression
  - Support Vector Machine (SVM)
  - Random Forest
- Model comparison with performance metrics.
- A prediction function that allows input of new patient data for instant prediction.

---

## ⚙️ Requirements
All required dependencies are listed in `requirements.txt`.

Install them using:
```bash
pip install -r requirements.txt
